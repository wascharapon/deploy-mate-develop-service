# File generated by github.com/posener/goaction. DO NOT EDIT.

name: deploy-mate-develop-service
description: "Creates action files for Go code By AgelCyber"
inputs:
  path:
    default: .
    description: "Path to main Go main package."
    required: true
  project:
    description: Project ID
    required: true
  location:
    description: Deploy Location ID
    required: true
  name:
    description: Deployment Name
    required: true
  image:
    description: Docker Image to deploy
    required: true

runs:
  using: "composite"
  steps:
    - uses: actions/setup-go@v2
      with:
        go-version: '1.21.6'
    - run: go run app/deploy_mate/main.go -location=${{ inputs.location }} -project=${{ inputs.project }} -name=${{ inputs.name }} -image=${{ inputs.image }}
      shell: bash
branding:
  icon: cloud
  color: red      